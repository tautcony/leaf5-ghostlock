# Build artifacts
*.so
*.o
*.a
*.pyc
__pycache__/

# Unified binary output tree (make / docker-build)
# Layout documentation: BUILD_OUTPUT.md (tracked)
/out/

# Legacy exploit-local paths (no longer written; keep ignored if present)
exploit/build/
exploit/assets/
exploit/preload.so
exploit/preload32.so
exploit/ghostlock32
exploit/test-programs/test_*
!exploit/test-programs/*.c

# Legacy probe paths
leaf5/probes/**/g32_*
leaf5/probes/**/g64_*
leaf5/probes/**/test_*
!leaf5/probes/**/*.c
!leaf5/probes/**/*.h
leaf5/stages/build/
leaf5/stages/**/build/

# Python: venv at repo root (uv sync)
.venv/
leaf5/.venv/
*.egg-info/
dist/
build/

# Lockfile may be committed; keep optional ignore off for uv.lock at root
# (uv.lock is tracked when present)

# NDK
/tmp/ndk_extract/

# IDE / OS
.vscode/
.idea/
.claude/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Kernel images and extracted binaries (local only)
vmlinux*
boot*.img
*.bin
leaf5/raw/kheaders/
leaf5/raw/kheaders.tar.xz
leaf5/raw/kernel_payload.bin
leaf5/raw/vmlinux*
