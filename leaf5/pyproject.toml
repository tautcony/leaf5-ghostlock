# Deprecated: project metadata moved to repo-root pyproject.toml
# Use from repository root:
#   uv sync
#   uv run leaf5-collect
#
# This file remains only so old `cd leaf5 && uv ...` workflows print a hint.

[project]
name = "leaf5-analysis-moved"
version = "0.0.0"
description = "MOVED: use repository root pyproject.toml"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
